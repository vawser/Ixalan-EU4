namespace = cal_setup

#---------------------------------------
# World
#---------------------------------------
country_event = {
	id = cal_setup.1
	title = cal_setup.1.title
	desc = cal_setup.1.desc
	picture = WOW_ASSASSINATION_eventPicture
	
    hidden = yes
    is_triggered_only = yes

    trigger = {
        NOT = { has_global_flag = completed_cal_setup }
        ai = no
    }

    immediate = {
        set_global_flag = completed_cal_setup
        
        # World State
        #hre_inheritable = yes
		#set_hre_religion = earth_mother
		#set_hre_heretic_religion = druidism
        #set_papacy_active = no
    }

    option = {
		name = cal_setup.1.option.a
    }  
}

#---------------------------------------
# Country
#---------------------------------------
country_event = {
	id = cal_setup.2
	title = cal_setup.2.title
	desc = cal_setup.2.desc
	picture = WOW_ASSASSINATION_eventPicture
	
    is_triggered_only = yes
    hidden = yes

    trigger = {
        NOT = { has_country_flag = completed_country_cal_setup }
    }
    
    immediate = {
        if = {
             limit = { ai = no }
             
             set_country_flag = trade_control_flag
        }
        
        discover_home_continent = yes
        
        set_country_flag = completed_country_cal_setup
    }
    
    option = {
		name = cal_setup.2.option.a
    }  
}
